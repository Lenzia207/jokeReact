import { makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({
    card: {
    minWidth: "100%",
    minHeight: "20vh",
    display: "flex",
    flexDirection: "row",
    justifyContent: "center", 
    alignItems: "center",

    padding: 2,
    borderRadius: 5
    },

    
}))

